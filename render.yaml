services:
  - type: web
    name: smart-ai-dubber
    env: python
    buildCommand: |
      pip install -r requirements.txt
      apt-get update && apt-get install -y ffmpeg
    startCommand: streamlit run app.py --server.port $PORT --server.address 0.0.0.0